<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Support Swift</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <nav
    class="bg-white shadow-lg flex justify-between items-center px-4 py-3"
    >
        <a href="/"><span class="text-2xl font-bold text-center"
            >Support Swift</span></a>
        <div class="flex gap-5">
            <a href="/login" class="text-gray-600 hover:text-gray-900">Login</a>
        </div>
    </nav>

    <div class="container px-4 py-8 flex flex-col gap-5 mx-auto">
        <h1 class="text-4xl font-bold text-center text-gray-800">
            Login
        </h1>
        
        <!-- login form -->
        <form action="/login" method="POST" 
        class="shadow-xl border-2 p-4 rounded-xl mx-auto w-full md:w-[500px] flex flex-col gap-2 font-semibold"
        >
            <label for="email">
                Enter your e-mail:
            </label>
            <input 
            type="email" 
            name="email" 
            id="email" 
            required
            class="border-2 border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-transparent">

            <label for="password">
                Enter your password:
            </label>
            <input 
            type="password" 
            name="password" 
            id="password" 
            required
            class="border-2 border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-transparent">

            <% if(error) { %>
                <p id="flash-message" class="text-red-600">
                    <%= error %>
                </p>
            <% } %>

            <button 
            type="submit" 
            class="bg-blue-600 text-white rounded-md p-2 mt-3 hover:bg-blue-700 transition duration-300 ease-in-out">
                Login
            </button>
        </form>
    </div>

</body>
</html>